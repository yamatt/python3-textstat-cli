[project]
name = "python3-textstat-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textstat==0.6.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
